import { MaturityStyleDirective } from './maturity-style.directive';

describe('MaturityColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new MaturityStyleDirective();
    // expect(directive).toBeTruthy();
  });
});
