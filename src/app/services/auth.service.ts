import { Injectable } from '@angular/core';
import * as users from '../../assets/data/users.json';

@Injectable({
  providedIn: 'root',
})
export class AuthService {
  constructor() {}

  // public loginAsync(username: string, password: string): Promise<> {}
}
